const colors = {
  blue: '#1565c0',
  blueDarker: '#003c8f',
  black: '#212121',
  blackGlass: 'rgba(0, 0, 0, .7)',
  gray: '#424242',
  white: '#fafafa',
}

const theme = {
  accent: colors.blue,
  accentDark: colors.blueDarker,
  background: colors.black,
  backgroundLight: colors.gray,
  backgroundSemiTransparent: colors.blackGlass,
  textColor: colors.white,
}

export default theme